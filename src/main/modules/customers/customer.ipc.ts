import type { IpcMain } from 'electron';

export function registerCustomerIpcHandlers(_ipcMain: IpcMain): void {
  void _ipcMain;
  // Customer workflows will be registered in a later implementation step.
}
